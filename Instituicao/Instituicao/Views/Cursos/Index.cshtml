@model IEnumerable<Instituicao.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CurNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurTotalPeriodos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurTurno)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CurNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurTotalPeriodos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurTurno)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CurID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CurID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CurID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
