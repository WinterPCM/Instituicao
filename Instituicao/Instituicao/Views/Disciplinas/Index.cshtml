@model IEnumerable<Instituicao.Models.Disciplina>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DisNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DisHoras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DisProfessor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DisNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DisHoras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DisProfessor.ProMatricula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DisID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DisID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DisID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
