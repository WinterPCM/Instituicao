@model IEnumerable<Instituicao.Models.Periodo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PerNumero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PerSala)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PerCurso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PerNumero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PerSala)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PerCurso.CurID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
